# File Integrity

Integrity checks calculate cryptographic hashes for files so you can compare a later copy with a known baseline.

Keep baseline hashes in a trusted location and compare them after changes or maintenance windows.
