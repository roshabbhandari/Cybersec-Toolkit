# Troubleshooting

If startup fails, verify Python and the packages listed in `requirements.txt`.

For module errors, run the smallest affected feature first and check the traceback. Missing files, permissions, DNS failures, and unavailable network services should be treated as normal error cases.
