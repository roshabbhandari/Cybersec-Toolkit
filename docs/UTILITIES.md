# Defensive Utilities

The toolkit now includes reusable helpers for input validation, URL normalization, IP/CIDR classification, redaction, hashing, encoding, file integrity, and report formatting.

All utilities are dependency-free unless they wrap an existing project dependency.
