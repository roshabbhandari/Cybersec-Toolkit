# macOS Notes

Create an isolated Python environment before installing dependencies. Network and filesystem results can differ based on macOS privacy controls and local permissions.

Use only authorized hosts for network checks.
