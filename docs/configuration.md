# Configuration

The current release is intentionally lightweight and keeps configuration close to the CLI prompts.

Future configuration files should contain non-secret preferences only. Credentials, private keys, and tokens must be supplied securely at runtime.
