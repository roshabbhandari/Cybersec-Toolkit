# Output Format

Interactive output is optimized for terminal readability. New modules should prefer predictable dictionaries, lists, or simple values internally so future report exporters can consume them.

Avoid printing secrets, private keys, passwords, or full authentication tokens.
