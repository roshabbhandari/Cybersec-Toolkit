# Linux Notes

Run the toolkit with the Python interpreter from your environment. Some local audit operations depend on file permissions and may produce different results for different users.

Keep log samples and generated reports outside the source tree when they contain sensitive information.
