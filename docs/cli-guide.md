# CLI Guide

Run `python main.py` to open the interactive terminal interface.

Use the numbered menus to select a defensive module. Press `0` to return or exit. Keep scan targets limited to systems you own or are authorized to assess.
