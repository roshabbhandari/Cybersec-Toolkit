# Security Headers Audit

The security-header checker reviews HTTP response headers that commonly improve browser-side protection.

A missing header is a finding to investigate, not automatically a vulnerability. Server behavior and application requirements should be considered before changing configuration.
