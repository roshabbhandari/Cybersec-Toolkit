# Release Process

Before a release, verify installation, exercise the main menu, review dependency changes, update the changelog, and confirm no test artifacts or secrets are included.

Tag releases only after the default branch is in a usable state.
